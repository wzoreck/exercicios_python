import random
num = random.random() # .random gera um número entre 0 e 1
print(num)
num1 = random.randint(1, 10) # podemos nesse caso gerar um número inteiro de 1 até 10, pq definimos assim!
print(num1)
